services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: receptfeed
      POSTGRES_USER: receptfeed
      POSTGRES_PASSWORD: receptfeed
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
